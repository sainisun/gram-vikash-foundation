import { getPublicDonationLedger, getPublicExpenseLedger } from "@/server/db";
const inr = (paise: number) => new Intl.NumberFormat("en-IN", { style: "currency", currency: "INR", maximumFractionDigits: 0 }).format(paise / 100);
export default async function LedgerPage() { const [donations, expenses] = await Promise.all([getPublicDonationLedger(30), getPublicExpenseLedger(30)]); return <main className="main"><p className="eyebrow">Public ledger</p><h1>Donation and expense records.</h1><section className="grid"><div className="ledger"><h2>Donations</h2>{donations.length ? donations.map(row => <p key={row.id}><strong>{row.displayName}</strong> · {row.programName} · {inr(row.amountPaise)}</p>) : <p>No successful donations published yet.</p>}</div><div className="ledger"><h2>Expenses</h2>{expenses.length ? expenses.map(row => <p key={row.id}><strong>{row.publicDescription}</strong> · {row.category} · {inr(row.amountPaise)}</p>) : <p>No expenses published yet.</p>}</div></section></main>; }

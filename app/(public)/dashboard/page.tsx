import { LiveSummary } from "@/components/transparency/LiveSummary";
import { getPublicTransparencySnapshot } from "@/server/db";
export default async function TransparencyDashboard() { const summary = await getPublicTransparencySnapshot(); return <main className="main"><p className="eyebrow">Live transparency dashboard</p><h1>Money shown in motion.</h1><LiveSummary initial={{ totalRaisedPaise: summary.totalRaisedPaise, totalSpentPaise: summary.totalSpentPaise, balancePaise: summary.balancePaise, generatedAt: summary.updatedAt.toISOString() }} /><p>Totals are calculated from source ledgers. The dashboard refreshes only while this page is visible, and you can manually refresh at any time.</p></main>; }

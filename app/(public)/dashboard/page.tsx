import { getPublicTransparencySnapshot } from "@/server/db";
const inr = (paise: number) => new Intl.NumberFormat("en-IN", { style: "currency", currency: "INR", maximumFractionDigits: 0 }).format(paise / 100);
export default async function TransparencyDashboard() { const summary = await getPublicTransparencySnapshot(); return <main className="main"><p className="eyebrow">Live-style transparency dashboard</p><h1>Money shown in motion.</h1><div className="grid"><div className="metric"><span>Raised</span><strong>{inr(summary.totalRaisedPaise)}</strong></div><div className="metric"><span>Spent</span><strong>{inr(summary.totalSpentPaise)}</strong></div><div className="metric"><span>Available</span><strong>{inr(summary.balancePaise)}</strong></div></div><p>Totals are calculated from source ledgers. The public API refresh layer is available at <code>/api/summary</code>.</p></main>; }

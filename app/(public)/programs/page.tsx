import Link from "next/link";
import { getPublicPrograms } from "@/server/db";
export default async function ProgramsPage() { const programs = await getPublicPrograms(); return <main className="main"><p className="eyebrow">Programs</p><h1>Work that can be followed, not just promised.</h1><p>Program content is published by authorized administrators. New programs appear here after publication.</p>{programs.length ? <section className="grid">{programs.map(program => <Link className="route-card" href={`/programs/${program.slug}`} key={program.id}><h2>{program.name}</h2><p>{program.shortDescription}</p></Link>)}</section> : <div className="ledger"><h2>No published programs yet.</h2><p>Authorized administrators can publish program content from the protected admin route.</p></div>}</main>; }

"use client";
import { useState } from "react";
export default function ExportPanel() { const [status, setStatus] = useState(""); async function prepare(scope: "donations" | "expenses" | "both") { const res = await fetch("/api/admin/exports", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ scope }) }); if (!res.ok) { setStatus("Export could not be prepared."); return; } const data = await res.json(); setStatus(`Prepared ${data.donationRows.length} donation and ${data.expenseRows.length} expense public-safe rows with audit evidence.`); } return <section className="ledger"><p className="eyebrow">Controlled financial export</p><h2>Prepare a public-safe export.</h2><p>Preparation is audit-logged. Direct browser download and private accounting exports require the A2 human review.</p><div style={{ display: "flex", flexWrap: "wrap", gap: 10 }}><button onClick={() => prepare("both")} type="button">Prepare both ledgers</button><button onClick={() => prepare("donations")} type="button">Donations only</button><button onClick={() => prepare("expenses")} type="button">Expenses only</button></div>{status ? <p aria-live="polite">{status}</p> : null}</section>; }

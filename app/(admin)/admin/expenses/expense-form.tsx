"use client";
import { FormEvent, useState } from "react";
export default function ExpenseForm() { const [status, setStatus] = useState(""); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const fd = new FormData(event.currentTarget); const response = await fetch("/api/admin/expenses", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ programId: null, amountPaise: Math.round(Number(fd.get("amount")) * 100), category: fd.get("category"), publicDescription: fd.get("publicDescription"), privateNotes: fd.get("privateNotes"), spentAt: new Date(String(fd.get("spentAt"))).toISOString() }) }); setStatus(response.ok ? "Expense recorded with an audit event. Public totals will update from source data." : "Expense could not be recorded. Check all required fields."); } return <form className="ledger" onSubmit={submit} style={{ display: "grid", gap: 12 }}><h2>Record an expense</h2><label>Amount (₹)<input name="amount" type="number" min="1" step="0.01" required /></label><label>Category<select name="category" defaultValue="operations"><option value="coaching">Coaching</option><option value="library">Library</option><option value="kanyadan">Kanyadan</option><option value="operations">Operations</option><option value="other">Other</option></select></label><label>Public description<input name="publicDescription" required /></label><label>Private internal notes<textarea name="privateNotes" /></label><label>Date spent<input name="spentAt" type="datetime-local" required /></label><button className="button" type="submit">Record expense</button>{status ? <p aria-live="polite">{status}</p> : null}</form>; }

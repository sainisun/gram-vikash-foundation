"use client";
import { FormEvent, useState } from "react";
type Member = { id: number; fullName: string; villageWard: string };
export default function OfflineDonationForm({ members }: { members: Member[] }) { const [status, setStatus] = useState(""); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const fd = new FormData(event.currentTarget); const response = await fetch("/api/admin/donations", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ memberId: Number(fd.get("memberId")), programId: null, amountPaise: Math.round(Number(fd.get("amount")) * 100), paymentMode: fd.get("paymentMode"), receivedAt: new Date(String(fd.get("receivedAt"))).toISOString(), notes: fd.get("notes"), idempotencyKey: crypto.randomUUID() }) }); setStatus(response.ok ? "Offline donation recorded with financial and audit evidence." : "Donation could not be recorded. Select a registered Member and complete all fields."); } return <form className="ledger" onSubmit={submit} style={{ display: "grid", gap: 12 }}><h2>Record cash or cheque</h2><label>Registered Member<select name="memberId" required defaultValue=""><option value="" disabled>Select Member</option>{members.map(member => <option key={member.id} value={member.id}>{member.fullName} · {member.villageWard}</option>)}</select></label><label>Amount (₹)<input name="amount" type="number" min="1" step="0.01" required /></label><label>Payment mode<select name="paymentMode" defaultValue="cash"><option value="cash">Cash</option><option value="cheque">Cheque</option></select></label><label>Received at<input name="receivedAt" type="datetime-local" required /></label><label>Private internal notes<textarea name="notes" /></label><button className="button" type="submit" disabled={!members.length}>Record donation</button>{!members.length ? <p>Register a Member first; anonymous public display never removes the internal Member link.</p> : null}{status ? <p aria-live="polite">{status}</p> : null}</form>; }

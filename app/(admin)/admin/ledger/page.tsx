import { getPublicDonationLedger, getPublicExpenseLedger } from "@/server/db";
import { requireManagedAdmin } from "@/lib/auth/session";
const inr = (paise: number) => new Intl.NumberFormat("en-IN", { style: "currency", currency: "INR", maximumFractionDigits: 0 }).format(paise / 100);
export default async function AdminLedgerPage() { await requireManagedAdmin(); const [donations, expenses] = await Promise.all([getPublicDonationLedger(100), getPublicExpenseLedger(100)]); return <main className="main"><p className="eyebrow">Admin · ledger review</p><h1>Review the public-safe financial projection.</h1><section className="grid"><div className="ledger"><h2>Donations</h2>{donations.length ? donations.map(row => <p key={row.id}>{row.displayName} · {inr(row.amountPaise)} · {row.programName}</p>) : <p>No entries yet.</p>}</div><div className="ledger"><h2>Expenses</h2>{expenses.length ? expenses.map(row => <p key={row.id}>{row.publicDescription} · {inr(row.amountPaise)}</p>) : <p>No entries yet.</p>}</div></section></main>; }

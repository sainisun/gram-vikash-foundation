import { getAuditLogEntries } from "@/server/db";
export default async function AuditLogPage() { const events = await getAuditLogEntries(100); return <main className="dashboard-page"><header className="dashboard-heading"><p className="eyebrow">Admin · audit evidence</p><h1>Who changed what.</h1><p>Audit entries help reviewers trace protected financial, program, export, and administrative actions. They are evidence, not a public activity feed.</p></header><section className="audit-panel"><div className="form-heading"><div><p className="eyebrow">Latest protected events</p><h2>Administrative record trail</h2></div><span className="stamp">{events.length} EVENTS</span></div>{events.length ? <div className="audit-list">{events.map(event => <div key={event.id}><strong>{event.action}</strong><span>{event.entityType}/{event.entityId} · actor #{event.actorUserId} · {new Date(event.createdAt).toLocaleString()}</span></div>)}</div> : <div className="record-empty"><span className="stamp">NO EVENTS</span><p>No administrative events are recorded yet. Once an approved protected action succeeds, its evidence will appear here.</p></div>}</section></main>; }
